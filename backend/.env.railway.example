# Railway Environment Variables Template
# Copy this to Railway dashboard or use railway variables set

# ============================================
# AUTO-GENERATED BY RAILWAY (Don't set these)
# ============================================
# DATABASE_URL=postgresql://...
# REDIS_URL=redis://...
# RAILWAY_ENVIRONMENT=production

# ============================================
# REQUIRED - Must set these manually
# ============================================

# Authentication (Generate secure random string)
JWT_SECRET=change-this-to-random-string-use-openssl-rand-hex-32

# Stripe Configuration (Get from https://dashboard.stripe.com)
STRIPE_SECRET_KEY=sk_test_51... # Use sk_live_ for production
STRIPE_WEBHOOK_SECRET=whsec_...  # Get after creating webhook endpoint
STRIPE_PRICE_PER_JOB=299         # Price in cents ($2.99)

# Application Settings
NODE_ENV=production
PORT=3001
FRONTEND_URL=https://your-frontend.vercel.app

# ============================================
# LLM CONFIGURATION (Choose at least one)
# ============================================

# Primary LLM Provider (choose one: anthropic, openai, groq)
LLM_PROVIDER=anthropic

# Anthropic Claude (Recommended for best quality)
ANTHROPIC_API_KEY=sk-ant-api03-...

# OpenAI GPT-4 (Alternative)
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4-turbo-preview

# Groq (Fast & Cheap alternative)
# GROQ_API_KEY=gsk_...

# ============================================
# OPTIONAL SETTINGS
# ============================================

# LLM Fallback Configuration
LLM_FALLBACK_PROVIDER=groq  # Fallback if primary fails
# GROQ_API_KEY=gsk_...      # Set if using as fallback

# Cost Control
LLM_COST_LIMIT=1.00  # Maximum cost per operation in USD

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX_REQUESTS=5    # Max requests per window

# Logging
LOG_LEVEL=info  # debug, info, warn, error

# ============================================
# PRODUCTION OPTIMIZATIONS (Optional)
# ============================================

# Redis Configuration
REDIS_MAX_RETRIES=3
REDIS_RETRY_DELAY=1000

# Queue Settings
MAX_CONCURRENT_JOBS=2
JOB_TIMEOUT_MS=300000  # 5 minutes

# Playwright Settings
PLAYWRIGHT_HEADLESS=true
PLAYWRIGHT_TIMEOUT=120000  # 2 minutes per board

# ============================================
# MONITORING (Optional but recommended)
# ============================================

# Sentry Error Tracking
# SENTRY_DSN=https://...@sentry.io/...

# LogDNA/Datadog
# LOG_DNA_KEY=...
# DATADOG_API_KEY=...